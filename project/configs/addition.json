{
    "logging_config": {
        "save_path": "./logs",
        "experiment_name": "addition_debug",
        "log_interval": 10,
        "checkpoint_interval": 100,
        "validation_interval": 10
    },
    "model_config": {
        "architecture": "DummyModel",
        "kwargs": {}
    },
    "optimizer_config": {
        "optimizer": "Adam",
        "kwargs": {
            "lr": 3e-4
        }
    },
    "dataset_config": {
        "train": {
            "dataset_name": "AdditionDataset",
            "kwargs": {
                "train_max_digits": 3,
                "test_max_digits": 6,
                "split": "train",
                "num_samples": 50000,
                "seed": 0
            },
            "num_workers": 1,
            "batch_size": 32
        }
    },
    "learner": "NextTokenLearner",
    "train_loss_config": {},
    "num_epochs": 1000,
    "seed": 0
}

